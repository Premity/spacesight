venv/
.venv/
node_modules/
__pycache__/
*.pyc
*.pyo
*.pyd
.env
models/*.pt
dist/
build/
.DS_Store
*.npz
*.zip
!test_data/*.npz
!test_data/*.zip
temp_*

# ml/ training notebooks + package are tracked. The dataset and weights are NOT
# (large; live on Drive — see ml/README.md). *.npz and models/*.pt above cover those;
# guard the data dir explicitly too:
ml/data/